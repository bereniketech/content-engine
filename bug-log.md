## [2026-03-24] Blog stream iterator type fix | What broke: Task 7 streaming routes referenced `stream.textStream`, causing TypeScript build failure. | Root cause: SDK version in this repo exposes async iteration events instead of `textStream`. | Fix: Switched both blog routes to `for await (const event of stream)` and extracted text from `content_block_delta` events. | File(s): app/api/blog/route.ts, app/api/blog/expand/route.ts
## [2026-03-24] Improve route nullable session fix | What broke: Type-check failed because improve API accessed `latestSession.input_type` when `latestSession` could be null. | Root cause: Incomplete null guard around optional session query result. | Fix: Switched conditional access to optional chaining (`latestSession?.input_type`). | File(s): app/api/improve/route.ts
## [2026-03-24] Social panel normalization type mismatch | What broke: Type-check failed after social UI implementation because normalized platform unions were assigned to specific platform slots. | Root cause: Generic platform normalizer returned a broad union type without key-constrained inference. | Fix: Added platform-key generic normalization and explicit safe normalization helpers before state updates. | File(s): components/sections/SocialPanel.tsx
## [2026-03-24] Social editable block lint rule violation | What broke: ESLint verify phase failed due `react-hooks/set-state-in-effect` error in SocialEditableBlock. | Root cause: Component synchronized draft content via `setState` inside `useEffect`, which is disallowed by enforced React hooks lint rule. | Fix: Removed effect-based sync and reset draft when entering edit mode so state updates happen in handlers instead of effect body. | File(s): components/sections/SocialEditableBlock.tsx
## [2026-03-25] Missing chai type definitions in verify | What broke: Build and type-check failed during `/verify` with `Cannot find type definition file for 'chai'`. | Root cause: TypeScript runtime expected `chai` type declarations in the environment but `@types/chai` was not installed in dev dependencies. | Fix: Installed `@types/chai` as a dev dependency to restore successful build/type-check flow. | File(s): package.json, package-lock.json
## [2026-03-25] Dashboard prerender Supabase client regression | What broke: `next build` failed while prerendering `/dashboard` with `Invalid supabaseUrl`. | Root cause: Dashboard page instantiated browser Supabase client during render, causing server-side prerender execution of browser-only configuration. | Fix: Moved Supabase client initialization into client-only flows (`useEffect` and restore handler) to avoid render-time construction. | File(s): app/dashboard/page.tsx
## [2026-03-25] API auth and prompt-hardening gaps | What broke: API routes accepted unauthenticated prompt requests and used unsanitized user strings in prompt construction paths. | Root cause: Route handlers used mixed auth patterns and lacked centralized sanitization before model prompt assembly. | Fix: Added centralized requireAuth/createSupabaseUserClient helpers, applied JWT guards to all API routes, added sanitizeInput/sanitizeUnknown utilities, and enforced prompt-input sanitization with middleware rate limiting. | File(s): lib/auth.ts, lib/sanitize.ts, middleware.ts, app/api/*/route.ts
## [2026-03-25] Session-backed workflow orchestration fix | What broke: Dashboard generation steps relied on sessionStorage and latest-session fallbacks, causing broken authenticated API calls, missing upstream asset handoff, stale summary counts, and inaccessible distribution/traffic workflow steps. | Root cause: The frontend and API layers did not share a deterministic session contract; browser calls used cookie auth while routes expected bearer headers, and multiple panels bypassed SessionContext entirely. | Fix: Added cookie-aware route auth, explicit session resolution helpers, live asset syncing in SessionContext, context-driven research/SEO/blog/social/images/distribution/traffic/flywheel panels, and missing dashboard routes with summary coverage for all generated assets. | File(s): lib/auth.ts, lib/session-assets.ts, lib/context/SessionContext.tsx, app/dashboard/**, components/**, app/api/**
## [2026-04-02] pdf-parse v2 API mismatch in parser utility | What broke: `parsePdf` assumed legacy callable `pdf-parse(buffer)` API, causing runtime/test instability once strict typing and deterministic tests were applied. | Root cause: Installed dependency version (`pdf-parse@2.x`) uses class-based `PDFParse` API, not function-call API. | Fix: Migrated parser implementation to `new PDFParse({ data }).getText()` with cleanup via `destroy()`, and updated unit tests to mock the class API deterministically. | File(s): lib/pdf-parse.ts, lib/__tests__/pdf-parse.test.ts
## [2026-04-02] Deep-research route resilience hardening | What broke: initial task-003 implementation lacked timeout/size guards and accepted unvalidated capability values from model output. | Root cause: route focused on feature path first and omitted defensive controls for external API latency and untrusted payload fields. | Fix: Added NotebookLM fetch timeout, request size validation, capability whitelist normalization, prompt evidence sanitization, and consistent topic-derivation research error handling. | File(s): app/api/data-driven/research/route.ts
## [2026-04-02] Data-driven article route validation hardening | What broke: the streaming article route treated bad session IDs and PDF parse failures as generic failures, and the prompt contract lacked automated coverage. | Root cause: initial task-005 implementation optimized for the happy path and left client-error mapping plus regression tests incomplete. | Fix: Added explicit invalid-PDF and missing-session responses, strengthened the article prompt requirements, and added prompt and route tests for JSON, multipart, auth, validation, and SSE persistence behavior. | File(s): app/api/data-driven/article/route.ts, lib/prompts/data-driven-article.ts, app/api/data-driven/article/route.test.ts, lib/prompts/data-driven-article.test.ts, jest.config.js
## [2026-04-02] Topic-session narrowing verification fix | What broke: repo verification could fail where dashboard views treated any session data containing a `topic` property as topic-workflow input, leading to undefined topic/audience/keyword accesses. | Root cause: shared session input unions were narrowed with property-existence checks that also matched data-driven session payloads. | Fix: Added an explicit `isTopicInputData` guard and applied it across the affected dashboard and panel components before reading topic-workflow fields. | File(s): types/index.ts, app/dashboard/blog/page.tsx, app/dashboard/page.tsx, app/dashboard/research/page.tsx, components/input/TopicForm.tsx, components/sections/FlywheelPanel.tsx, components/sections/TrafficPanel.tsx
## [2026-04-02] SEO+GEO route contract hardening | What broke: initial SEO+GEO task implementation could return inconsistent schema cardinality and accepted non-UUID session IDs while lint/verify signals were polluted by external `.venv` scanning. | Root cause: schema normalization checks were too permissive for secondary keywords/FAQ counts and input validation did not constrain session identifier format. | Fix: enforced strict SEO/GEO output validation, added UUID validation for `sessionId`, expanded route tests for failure/fallback branches, and scoped lint validation to changed files while documenting global verify blockers. | File(s): app/api/data-driven/seo-geo/route.ts, app/api/data-driven/seo-geo/route.test.ts, lib/prompts/seo-geo.ts, types/index.ts, .spec/data-driven-pipeline/tasks/task-006.md, .claude/CLAUDE.md
## [2026-04-02] Multi-format route validation and contract hardening | What broke: initial task-007 implementation allowed weak `seoGeo` payloads and omitted route-level contract tests, risking invalid downstream generation and inconsistent responses. | Root cause: early implementation focused on happy-path generation before strict input-shape enforcement and full API edge-case coverage. | Fix: added strict `seoGeo` `{ seo, geo }` validation, object-body validation, per-field required checks, four-asset persistence assertions, and prompt/route regression tests; response now includes `sessionId` plus all four saved assets. | File(s): app/api/data-driven/multi-format/route.ts, app/api/data-driven/multi-format/route.test.ts, lib/prompts/multi-format.ts, lib/prompts/multi-format.test.ts, .spec/data-driven-pipeline/tasks/task-007.md
## [2026-04-02] Data-driven session launch and PDF handoff fix | What broke: task-009 initially created a data-driven session but left the form unreachable, routed successful submits to a missing page, and dropped staged PDFs before the pipeline could use them. | Root cause: the first pass implemented the input component in isolation without wiring the dashboard entry path, route landing page, or cross-route PDF state handoff. | Fix: integrated `DataDrivenForm` into the dashboard launcher, added `/dashboard/data-driven` as the session landing page, introduced `pendingDataDrivenFile` in `SessionContext`, and preserved it during immediate post-submit `loadSession()` calls. | File(s): app/dashboard/page.tsx, app/dashboard/data-driven/page.tsx, components/input/DataDrivenForm.tsx, lib/context/SessionContext.tsx, lib/data-driven-form.ts, lib/data-driven-form.test.ts, types/index.ts, .spec/data-driven-pipeline/tasks/task-009.md, .claude/CLAUDE.md
## [2026-04-02] Data-driven pipeline regenerate and SSE completion fix | What broke: regenerating a completed step could leave downstream `dd_*` assets in memory, causing stale reload restoration; article SSE parsing could also drop trailing stream events when the final chunk lacked a delimiter. | Root cause: regenerate initially reset only step runtime state (not downstream asset cache), and stream parsing processed only delimiter-terminated blocks. | Fix: added downstream asset-type mapping and filtered `SessionContext` assets on regenerate; added post-loop SSE buffer flush to process final pending event payload. | File(s): app/dashboard/data-driven/page.tsx, lib/data-driven-pipeline.ts, lib/data-driven-pipeline.test.ts, .spec/data-driven-pipeline/tasks/task-010.md, .claude/CLAUDE.md
## [2026-04-02] Data-driven output page contract and copy alignment fix | What broke: initial task-011 output pages allowed partial X campaign payloads and could copy content that did not match visible output mode (newsletter plain-text view) or duplicate hashtag payloads in X post copy. | Root cause: first-pass page implementations prioritized route availability before tightening output-shape validation and mode-aware clipboard logic. | Fix: enforced strict 10-post/10-thread validation for X campaign display, switched post copy to content-as-authored payload, made newsletter body copy mode-aware, and improved newsletter full-HTML copy rendering from markdown-like content. | File(s): app/dashboard/data-driven/newsletter/page.tsx, app/dashboard/data-driven/x-campaign/page.tsx
## [2026-04-02] Data-driven summary X campaign count mismatch | What broke: SummaryPanel displayed `dd_x_campaign` as 1 generated piece even when multiple campaign posts existed. | Root cause: count logic handled `social_x` only and fell back to `defaultCount` for `dd_x_campaign`. | Fix: Added a `dd_x_campaign` case in `getActualCount` to count `content.posts` when present. | File(s): components/dashboard/SummaryPanel.tsx
## [2026-04-03] Lint verification traversed virtualenv artifacts | What broke: task-013 lint verification surfaced non-project warnings from `.venv` and blocked clean verification output. | Root cause: ESLint global ignores did not exclude workspace-local virtualenv and generated coverage directories. | Fix: Added `.venv/**` and `coverage/**` to ESLint global ignores so lint targets only project source. | File(s): eslint.config.mjs
